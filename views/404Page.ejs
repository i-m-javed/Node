<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head', { pageTitle }) %>

  <body
    class="bg-gradient-to-br from-gray-50 to-gray-100 text-gray-800 font-sans min-h-screen flex flex-col pattern-dots"
  >
    <!-- Header -->
    <%- include('partials/nav',{pageTitle}) %>

    <!-- Main Content -->
    <main
      class="flex-grow flex items-center justify-center text-center px-4 py-12"
    >
      <div
        class="max-w-2xl w-full bg-white rounded-2xl shadow-xl p-8 transform hover:scale-105 transition-transform duration-300"
      >
        <div class="relative">
          <!-- Decorative circles -->
          <div
            class="absolute -top-4 -left-4 w-24 h-24 bg-red-100 rounded-full opacity-50"
          ></div>
          <div
            class="absolute -bottom-4 -right-4 w-32 h-32 bg-red-100 rounded-full opacity-50"
          ></div>

          <!-- Main content -->
          <div class="relative z-10">
            <h2
              class="text-8xl font-extrabold text-red-600 mb-4 animate-bounce"
            >
              404
            </h2>
            <p class="text-2xl text-gray-700 mb-8 leading-relaxed">
              Oops! The page you're looking for seems to have taken a vacation.
            </p>
            <div class="space-y-4">
              <a
                href="/"
                class="inline-block bg-red-500 hover:bg-red-600 text-white font-semibold py-4 px-8 rounded-lg shadow-lg hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1"
              >
                Return to Home
              </a>
              <p class="text-sm text-gray-500 mt-4">
                Don't worry, you can always find your way back home!
              </p>
            </div>
          </div>
        </div>
      </div>
    </main>

    <!-- Footer -->
    <%- include('partials/footer') %>
  </body>
</html>
